{
  "name": "jung-app",
  "private": true,
  "scripts": {
    "test": "npm run check:syntax && npm run check:html && npm run check:smoke",
    "check:syntax": "node --check app.js && node --check attribution.js && node --check landing.js && node --check root-redirect.js && node --check miniapp-boot.js",
    "check:html": "html-validate '*.html'",
    "check:smoke": "node tests/landing-smoke.mjs"
  },
  "devDependencies": {
    "html-validate": "11.5.6"
  }
}
